import 'package:flutter/material.dart';

class FollowedPage extends StatelessWidget {
  const FollowedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Followed Page')),
    );
  }
}
